{
  "name": "@sdkwork/aiot-mini-program",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "description": "SDKWork AIoT WeChat Mini Program application.",
  "dependencies": {
    "@sdkwork/sdk-common": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "scripts": {
    "dev": "pnpm dev:standalone",
    "dev:standalone": "pnpm exec sdkwork-app dev --root ../.. --deployment-profile standalone",
    "dev:cloud": "pnpm exec sdkwork-app dev --root ../.. --deployment-profile cloud",
    "stop": "pnpm exec sdkwork-app stop --root ../..",
    "build": "pnpm exec sdkwork-app build",
    "test": "pnpm exec sdkwork-app test",
    "check": "pnpm exec sdkwork-app check",
    "verify": "pnpm exec sdkwork-app verify",
    "clean": "pnpm exec sdkwork-app clean",
    "_sdkwork:client:mini-program": "node scripts/mini-program-dev.mjs",
    "_sdkwork:build": "node scripts/build.mjs",
    "_sdkwork:test": "node --test scripts/*.test.mjs",
    "_sdkwork:check": "node ../../../sdkwork-specs/tools/check-app-manifest-standard.mjs --root . && node ../../../sdkwork-specs/tools/check-source-config-standard.mjs --root . && node --check src/app.js",
    "_sdkwork:verify": "pnpm run _sdkwork:check && pnpm run _sdkwork:test && pnpm run _sdkwork:build",
    "_sdkwork:clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "exports": {
    ".": {
      "import": "./src/app.js",
      "default": "./src/app.js",
      "types": "./src/app.js"
    }
  }
}
